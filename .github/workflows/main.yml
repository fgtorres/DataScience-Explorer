on: 
  push: 
    branches:
      - main
name: 🎉 Deploy Dashboard
jobs:
  prepare:
    name: 🚀 Deploy
    runs-on: ubuntu-latest
    needs: prepare
    steps:
    - name: 🚚 Pegando codigo mais recente...
      uses: actions/checkout@v2